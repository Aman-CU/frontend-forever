# Frontend Forever — Claude Code

## Start Here

Read `AGENTS.md` before anything else. It contains all rules, context file read order, available skills, environment variables, and architecture decisions.

## Context Files

All project context lives in `context/`. AGENTS.md tells you what to read and in what order.

## Current Build Status

Check `context/progress-tracker.md` for what's done, what's in progress, and what to build next.
