---
title: "Bundle Size & Code Splitting"
description: "See how splitting a bundle by route or component lets the browser download only the code a page actually needs, and how to measure what's bloating it."
category: "performance"
difficulty: "intermediate"
order: 2
whatsHappening: "A naive build ships one giant JS file containing every route, every component, and every dependency the app will ever need — so a user landing on the login page downloads the checkout flow, the admin dashboard, and everything else, unused. Code splitting breaks that single bundle into smaller chunks: one per route (loaded only when that route is visited) plus a shared chunk for code multiple routes actually use in common (React itself, a UI library, shared utilities). The core signal the bundler starts from is usage count — which modules appear in more than one route's dependency graph — though a real bundler layers extra thresholds (minimum size, maximum request count) on top before it actually splits something out."
keyInsight: "A module doesn't belong in the shared chunk just because it's 'important' — the starting signal is that more than one route's code actually imports it. A real bundler still weighs that against size/request thresholds before acting on it, but a module imported by exactly one route never qualifies for the shared chunk at all, regardless of those thresholds."
memoryHook: "Route chunks are what only that page needs; the shared chunk is what every page needed anyway — splitting just stops paying for the second one twice."
inRealLife: "Like a shared office supply closet versus each department's own equipment — printer paper (used by everyone) lives in the shared closet once, while the design team's drawing tablets stay in the design team's own room instead of being duplicated everywhere."
---

## The problem: one bundle for every route

Without splitting, every route's code — and every dependency each route pulls in — lands in a single file the browser must download and parse before anything on the page can run, even routes the user never visits in that session.

## Route-based splitting

Most modern frameworks split automatically at the route boundary: visiting `/login` only downloads the login route's chunk, not `/checkout`'s or `/admin`'s. In Next.js's App Router, this happens per-page by default — each route segment becomes its own chunk without extra configuration.

## Shared chunks: the modules multiple routes actually use

If `/login`, `/checkout`, and `/admin` all import React and a shared `Button` component, the bundler pulls those into one shared chunk instead of duplicating them into all three route chunks. The starting signal is usage count: a module used by exactly one entry point always stays local to it, and only a module used by more than one entry point is even a *candidate* for the shared chunk.

> **This is the simplified core idea, not a real bundler's full algorithm.** Tools like webpack's `SplitChunksPlugin` don't split out every multi-use candidate automatically — they gate it behind extra thresholds too (a minimum chunk size, a cap on how many parallel requests a page can make), so a tiny module used twice might still get inlined into both routes rather than pulled into its own chunk. Usage count decides *eligibility*; size and request-count thresholds decide whether splitting it out is actually worth the extra network request.

## Component-level splitting with dynamic import

Splitting isn't only per-route — a single heavy component (a rich text editor, a charting library, a modal that's rarely opened) can be split out on its own with a dynamic import, so its cost is only paid when that component actually renders:

```js
const HeavyEditor = dynamic(() => import("./HeavyEditor"));
```

This is the same underlying mechanism as route splitting, just triggered by a component boundary instead of a route boundary.

## Measuring what's bloating the bundle

A bundle analyzer visualizes each chunk as a proportionally-sized box, making it easy to spot a single oversized dependency (a full date library pulled in for one formatting call, an icon set imported in full instead of per-icon) that's worth splitting out or replacing with a lighter alternative.
