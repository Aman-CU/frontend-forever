# Progress Tracker

Update this file after every completed feature. Any AI agent reading this should immediately know what is done, what is in progress, and what is next.

---

## Current Status

**Phase:** Phase 1 — Homepage, in progress
**Last completed:** 06 Feature Highlights Section — ran the `architect` skill (manually, since project-local skills aren't registered with the Skill tool) before building. The build-plan's literal spec is a flat 2×2 grid of compact cards; asked the user for creative direction on improving it and built a bento layout instead: one larger "hero" card for "Concepts You Can See" (the platform's actual differentiator per `AGENTS.md`) on top, full-width, with the other 3 cards ("Real Engineering Challenges", "Interview-Ready Questions", "Build Real Things") in an equal-width row below — single column on mobile in the same top-to-bottom order. Three other decisions resolved with the user up front: (1) independent/fresh color mapping rather than mirroring Feature 05's Simulate/Challenge/Interview/Build color assignments — Concepts You Can See=`accent` (teal), Real Engineering Challenges=`info` (blue), Interview-Ready Questions=`premium` (purple), Build Real Things=`success` (green); (2) brand-new, smaller dedicated mini-visual components per card rather than reusing/shrinking Feature 05's `*Preview` components, to avoid coupling this feature to Feature 05's internals; (3) plain one-shot `whileInView` stagger entrance only (per build-plan's literal "fade in with stagger on scroll enter") — no continuous/infinite loops on this section, unlike Feature 05's ambient-motion treatment.
  - New components: `src/components/homepage/FeatureHighlightsSection.tsx` (FEATURES data + section wrapper, splits the array into `hero` + `rest` and renders the bento layout) and `src/components/homepage/feature-highlights/` — `FeatureCard.tsx` (shared themed card shell, `hero` boolean prop switches between the hero's horizontal layout and the supporting cards' vertical layout), `SimulatorMiniVisual.tsx`, `CodeSnippetMiniVisual.tsx`, `QAMiniVisual.tsx`, `EditorMiniVisual.tsx` (one static, non-animated mini-visual per card, matching the build-plan's "simulator preview / code snippet / Q&A / Monaco editor" descriptions but new and intentionally simpler than Feature 05's full-size previews).
  - One real decision during build: `EditorMiniVisual` was first drafted with a true Monaco-style fixed-dark (`#1E1E1E`) code block per `ui-rules.md`'s documented Monaco exception, paired with `text-text-inverse`. Caught before committing that `--color-text-inverse` flips to near-black in dark mode (it's "inverse of the page," not "fixed light"), which would have made that text invisible against the fixed-dark background once dark mode was toggled — same class of token-misuse bug as elsewhere in this project, just caught at design time instead of via a screenshot this time. Fixed by dropping the special-cased dark mockup and matching the same light `bg-surface-secondary` + `text-text-secondary` code-block treatment already used by the other 3 mini-visuals, which keeps all 4 cards visually consistent as a side benefit.
  - Verified: typecheck (`npx tsc --noEmit`) and lint (`npx eslint`) clean on all new files; Playwright screenshots at desktop (1440px) and mobile (390px) × light/dark confirmed the bento layout, per-card color theming, and stacking order all render correctly; confirmed no new horizontal overflow introduced (`section.scrollWidth === section.clientWidth` at 390px — the only mobile-page-level overflow is the pre-existing, already-documented `CompanyLogosStrip` scroll row from Feature 04, unrelated to this feature).
  - Git: the working branch at the time (`chore/05-ui-rules-update`) turned out to already be merged into `develop` via PR #8 by the time this feature was built — moved the new files onto a fresh `feature/06-feature-highlights-section` branched off the now-current `origin/develop` (which also already includes Feature 05 via PR #7) before committing, per the project's branch-per-feature rule. Committed locally; not yet pushed/PR'd.
  - **Follow-up (full content + visual rework, same session, before any PR):** user rejected the first pass as reusing Feature 05's exact examples (Event Loop, `debounce.js`, a counter) and asked for richer, more varied content — naming the real interview-prep collections (FF 75, FF JavaScript Pro, FF React Special), showing that challenges run in a real code editor, and making clear interview prep is premium/company-documented questions, not generic MCQs — plus a creative request to visualize the platform as a graph with "Frontend Forever" at the center and the other features connected by glowing/traveling lines.
    - Added `PlatformGraph.tsx` (`feature-highlights/`) as a new hero visual rendered above the 4 detail cards: a hub-and-spoke diagram — a center "FF" node (`bg-surface` circle + soft `blur-xl` accent glow) with 4 cardinal-position nodes (top/right/bottom/left, one per feature, themed/icon-matched to the card below it), connected by SVG `<line>` pairs (a wide low-opacity line for glow + a thin bright line on top — no `<feGaussianBlur>` filter, kept simple/cheap) plus a small `motion.circle` per line animating `cx`/`cy` from the outer node to center on an infinite loop, staggered per line. This is a deliberate, explicitly-requested exception to this same feature's earlier "no continuous motion" decision — that decision was scoped to the 4 cards, not this new centerpiece visual. Confirmed via screenshot that Tailwind v4's auto-generated `stroke-*`/`fill-*` utilities work correctly against this project's custom `--color-accent`/`info`/`premium`/`success` theme variables, the same way `bg-*`/`text-*` already do elsewhere — no separate SVG-specific token setup was needed.
    - Rewrote all 4 mini-visuals with fresh, non-overlapping examples and renamed the files to match (old `SimulatorMiniVisual`/`CodeSnippetMiniVisual`/`QAMiniVisual`/`EditorMiniVisual` deleted, not kept behind a flag): `SpecificityMiniVisual.tsx` (CSS Specificity selector-vs-bar-chart, replacing the Event-Loop-flavored visual — Event Loop was already heavily used in `Hero.tsx`/`HowItWorksSection`, so this card now shows a concept that hasn't appeared anywhere else yet, while its description text still names all 4 simulators by name), `ChallengeEditorMiniVisual.tsx` (a multi-tab editor mockup — `VirtualList.tsx` / `useVirtualizer.ts` — solving a real frontend system-design problem, "render 10,000 rows at 60fps", instead of the reused `debounce.js` snippet), `InterviewPrepMiniVisual.tsx` (collection pills for FF 75/FF JavaScript Pro/FF React Special + one `Lock`-badged "Premium · Asked at Meta" question, replacing the plain unattributed Q&A bubble), `ProjectEditorMiniVisual.tsx` (`KanbanBoard.tsx` instead of `Counter.tsx`, a more portfolio-worthy project brief).
    - Added a `shortLabel` field to the shared `FeatureHighlight` type (`FeatureCard.tsx`) — short graph-node labels ("Simulators", "Challenges", "Interview Prep", "Projects") distinct from each card's full marketing `title` ("Concepts You Can See", etc.), since the full titles don't fit legibly inside the graph's small circular nodes.
    - Re-verified after the rework: typecheck/lint clean; Playwright screenshots desktop/mobile × light/dark confirming the graph's lines/glow/traveling dots render in the correct per-node theme color in both themes, all 4 rewritten card visuals read correctly, and the graph compresses to a legible (if tight) size on a 390px mobile viewport without enlarging the page beyond the same pre-existing `CompanyLogosStrip` mobile overflow baseline (`document.documentElement.scrollWidth` 440 vs `clientWidth` 390 — unchanged from before this feature existed).
  - **Follow-up 2 (graph redesign to a real mind-map style, same session, before any PR):** user shared a GreatFrontEnd-style reference screenshot (a center card listing "GFE 75 / Blind 75 / Front end system design" with curved lines branching out to ~10 topic pills on both sides) and said the prior 4-node hub-and-spoke version didn't show enough of the platform — explicitly said the 4 example names given earlier (FF 75/JavaScript/React/System Design) were "an example not to just put," and to use creativity to actually showcase platform capability. Read `context/project-overview.md` for the first time this session (hadn't been needed until now) to ground the redesign in real product facts rather than invented ones, and caught a real naming bug in the process: `InterviewPrepMiniVisual`'s collection pills said "FF JavaScript Pro"/"FF React Special" (the user's own casual paraphrase from an earlier message), but `project-overview.md`'s Interview Prep section and `code-standards.md`'s `INTERVIEW_COLLECTIONS` constant both give the real canonical names as **FF 75 / FF JavaScript / FF React / FF System Design** (no "Pro"/"Special" suffixes) — fixed the card's `COLLECTIONS` array and `FeatureHighlightsSection.tsx`'s matching description text to the canonical names, and the user's latest message (giving the same 4 names without suffixes) confirmed this correction was right.
    - Rebuilt `PlatformGraph.tsx` to mirror the reference's actual structure rather than a generic radial hub: the center card now lists the platform's 4 real flagship collections (same canonical names, numbered 1–4, mirroring GFE's own numbered center-card rows) and 8 real concept-category pills branch out from both sides via curved lines — pulled directly from `project-overview.md`'s documented "Concept Categories" list (JavaScript Runtime, Browser Internals, CSS, TypeScript on the left; React, Accessibility, Performance, System Design on the right) rather than inventing topic names. Two of the curves (the topmost on each side) are rendered with a teal-free `xp`→`streak` (amber→orange) linear gradient as a featured-path accent, echoing the reference image's own yellow-orange highlighted curves while staying within this project's existing token system (reusing the gamification XP/streak tokens purely for hue, same "reuse outside literal meaning, scoped to one component" precedent as `HeroSimulatorPreview`/`StepSection`).
    - **Real measurement bug caught and fixed via the established pattern, not by eyeballing:** the first version of the curves used hand-guessed fixed percentages for where each line should start/end (e.g. "pill right edge is around x=26%"), copying the radial graph's earlier static-percentage approach. Screenshots showed visible gaps between the lines and both the pills (pill text widths vary — "CSS" vs "JavaScript Runtime" — so no single constant fits all 4 rows) and the center card. Measured the actual rendered positions directly (Playwright `getBoundingClientRect()` against the graph container) and confirmed the guesses were off by several percentage points in both directions. Fixed properly — not by re-guessing closer numbers — by adopting this same codebase's own existing precedent for exactly this problem: `AnimatedHeading.tsx`'s `useLayoutEffect` + `getBoundingClientRect()` + a `window resize` listener, used there to measure real letter positions instead of hardcoding pixel offsets. `PlatformGraph` now measures each topic pill's real edge and the center card's real edges on mount (and on resize) and computes the SVG curve endpoints from those measurements, so the lines touch every pill and the card exactly regardless of text length, font rendering, or container width.
    - Re-verified after the redesign: typecheck/lint clean; Playwright screenshots confirming the curves now precisely connect pills to the card in both light and dark mode, the highlighted gradient curves render correctly, mobile (390px) falls back to a simple stacked collections-card + wrapped topic-tag-cloud layout (no curves/absolute positioning — a wide branching diagram doesn't translate to a narrow viewport, same "simplify on mobile, don't replicate 1:1" precedent as `ui-rules.md`'s existing "Hero simulator is simplified... on mobile" rule), and no new horizontal overflow was introduced (section `scrollWidth === clientWidth` at 390px; the only page-level overflow is still the pre-existing, unrelated `CompanyLogosStrip` row).
  - **Follow-up 3 (fix two real rendering bugs + add continuous motion, same session, before any PR):** user reported the graph looked "very static," lines were "uneven" with "very bold" stroke, and lines weren't connecting well to the center card. Investigated rather than just re-tuning numbers — found two real, distinct bugs, not style nitpicks:
    1. **Root cause of "uneven"/"bold" lines (and, found while fixing it, the traveling dots rendering as ellipses, not circles):** the SVG used a *square* `viewBox="0 0 100 100"` stretched with `preserveAspectRatio="none"` onto the container's *16:9* (`aspect-video`) box. Stretching a square coordinate system non-uniformly onto a non-square box scales x and y by **different factors** — which distorts anything that isn't pure position: stroke width renders thicker on steeper curve segments than flatter ones (the "uneven" look), and a perfect-circle `r` renders as an ellipse. Real fix, not a numbers tweak: made the viewBox's own aspect ratio match the container's (`VIEWBOX_WIDTH = 100`, `VIEWBOX_HEIGHT = (100 * 9) / 16 = 56.25`, both now exported from `graphData.ts`), with a `toSvgY()` helper converting the existing percent-of-height values into this corrected SVG-space scale at every point a y-coordinate enters a `<path>` `d` attribute or a `<circle>` `cx`/`cy`. Verified via `getBoundingClientRect()` on each traveling dot that width and height are now identical (confirming true circles, not ellipses) — not just a visual screenshot guess. Also added `vectorEffect="non-scaling-stroke"` + `strokeLinecap="round"` to every line and lowered the widths (2px highlighted / 1.25px normal, now in real screen pixels, unaffected by container size) for a thinner, crisper look addressing "very bold" directly.
    2. **Root cause of "not connect well":** the `useLayoutEffect` measurement ran while the pills/card were still sitting at their Framer Motion `initial` (pre-entrance) transform — they were defined with a `y: 16` slide-in offset, and Framer Motion applies that transform synchronously on mount, before the layout effect's `getBoundingClientRect()` call. So every line was computed against the *pre-animation* position, not the *settled* position the user actually sees — a real measurement-timing bug, not an off-by-a-few-percent guess. Fixed by dropping the `y` offset from `fadeInVariants` (now opacity-only for the outer container, the pills, and implicitly the card, which never had its own offset) — position is now identical in both the "hidden" and "visible" animation states, so it no longer matters when the measurement runs. Also added a `requestAnimationFrame` follow-up measurement after the synchronous one, as a safety net against post-paint width shifts (e.g. web font swap).
    - **"Feel alive" additions**, kept deliberately small (continuous-loop budget similar to Feature 05's per-section count): a soft pulsing glow (`opacity`/`scale` loop) behind the center collections card, a small `Sparkles` icon doodle floating/rotating above it, and a small glowing dot continuously traveling along each of the two highlighted (gradient) curves — computed via a cubic-bezier point-sampler (`sampleLink` in `graphData.ts`, matching the same control points as the visible `linkPath` curve, so the dot visually follows the actual curve shape rather than cutting a straight line across it) rather than a naive straight-line `cx`/`cy` interpolation.
    - **File-size follow-through:** the fixes above (plus the new motion) pushed `PlatformGraph.tsx` to 273 lines, over `code-standards.md`'s 200-line limit. Split immediately rather than leaving it oversized: pure math/data (`graphData.ts` — topic/collection arrays, the bezier sampler, `linkPath`, `cardEntryY`, the new viewBox constants), the measurement logic (`useGraphMeasurements.ts`, a dedicated hook — same DOM-measurement idiom as `AnimatedHeading.tsx`), and the small repeated card UI (`CollectionsCard.tsx`, used by both the desktop graph and the mobile fallback). `PlatformGraph.tsx` itself is back down to 183 lines, just orchestration + JSX.
    - Re-verified: typecheck/lint clean on all 4 files; Playwright screenshots desktop/mobile × light/dark confirming thin even-width lines that visibly terminate exactly on both the pills and the card, circular (not elliptical) traveling dots, and the new glow/sparkle/dot motion all working; no new overflow.
  - **Follow-up 4 (`/recover` — fixed a real dashed-line + overflow bug, then added more motion, same session, before any PR):** user ran the `recover` skill against two visible defects: graph lines rendering dashed instead of solid, and the graph visually overlapping/bleeding into the bento cards below it. Diagnosed as Failure Mode 1 (a specific, isolated, traceable bug — not a polluted session or a wrong foundation) and found two real root causes from the immediately preceding follow-up's own changes, not new mystery bugs:
    1. **Dashed lines:** the curves used Framer Motion's `pathLength` draw-in (a `stroke-dasharray`/`stroke-dashoffset` technique sized to the path's length at the time it's first computed) on a `d` attribute that gets **recomputed live** from `useGraphMeasurements` — and the previous follow-up had just added a second `requestAnimationFrame` measurement pass as a safety net, guaranteeing the `d` updates again shortly after mount. The cached dash spacing (sized to the old geometry) no longer matched the new path's actual length, so the dash pattern repeated unevenly along it instead of drawing one solid line.
    2. **Overlap with content below:** the same follow-up had added `overflow-visible` to the SVG (to stop the center glow's blur from clipping) — which removed the clipping that would otherwise have contained any path geometry briefly extending past the box during a measurement update, letting the bad-looking dashed tail paint directly over the section below instead of being invisibly clipped.
    - Fix: dropped `pathLength`/`pathVariants` entirely from the curve paths (they're now a plain opacity fade via the same `fadeInVariants` already used elsewhere — geometry-independent, so it can never desync from a changing `d`), and removed `overflow-visible` so the container clips normally again. Confirmed before writing any further motion that this alone fixed both reported symptoms.
    - **Then, per explicit follow-up request, added two more motion features on top of the fix** (not separate bugs — new asks): (1) every line (not just the 2 featured/gradient ones) now has its own continuously traveling dot, each row given a distinct color via the existing 5-token hue system (`DOT_FILL_CLASSES` in `graphData.ts`: `fill-xp`/`fill-accent`/`fill-info`/`fill-premium`, one per row, mirrored left/right), each with a different `duration` and `delay` (`dotDelay` per line, offset between the left and right sides too) so none of the 8 dots move in lockstep — deliberately "not in sync," per the request; (2) a `CardBorderGlow` component — two synced layers (a blurred halo + a crisp core, same `animate`/`transition`) that continuously travel clockwise around the center collections card's perimeter via 5-keyframe `top`/`left` percentage cycles (corner → corner → corner → corner → back to start, `ease: "linear"`, `repeat: Infinity`), rendered as a sibling inside the same already-`position: absolute` card wrapper (which already qualifies as the containing block for its children, no extra `relative` class needed).
    - **File split, again:** the per-line path+dot logic was extracted into its own `GraphLine.tsx` (one component rendering both the `<path>` and its `<motion.circle>` for a single line, reused 8× instead of duplicating the JSX) — this also simplified `PlatformGraph.tsx` itself back down to 143 lines despite the added features, comfortably under the 200-line limit without needing the precomputed `leftHighlight`/`rightHighlight` consts from the previous round (every line, not just 2, now computes its own sample inline inside `GraphLine`).
    - Re-verified: typecheck/lint clean across all files; Playwright screenshots confirmed solid (non-dashed) lines, zero overlap with the section below, 4 distinctly-colored dots per side at visibly different positions along their lines (confirming independent timing), and 3 time-spaced crops of the center card confirming the border glow dot actually travels around the full perimeter (bottom edge → top edge → bottom edge across the captured frames) rather than just sitting decoratively in one spot; no new overflow.
  - **Follow-up 5 (`/review`, then fixed everything found, same session, before any PR):** ran the project's `review` skill against the finished feature (manually, since project-local skills aren't registered with the Skill tool). Found and fixed 6 issues, all confirmed resolved afterward — not just reported:
    1. **Critical, confirmed via a live Playwright console check (not assumed):** every one of `GraphLine.tsx`'s 8 traveling-dot circles threw `Error: <circle> attribute cx/cy: Expected length, "undefined"` once on mount (16 real console errors per page load, reproducible in every theme/viewport tested, including mobile where the desktop graph is `display:none` but still mounted). Root cause: each circle's `animate` drove `cx`/`cy` via a keyframe array, but `initial` only specified `opacity: 0` — no `cx`/`cy` baseline — so Framer Motion had no defined starting value to fall back on before the first real animated frame. Fixed by giving every circle an explicit `initial={{ cx: sample.cx[0], cy: sample.cy[0], opacity: 0 }}`. Re-ran the same console check after the fix: zero errors/warnings across both themes and both viewports.
    2. **Important:** `InterviewPrepMiniVisual.tsx` had its own local `COLLECTIONS` array, an exact duplicate of `graphData.ts`'s exported `COLLECTIONS` — same 4 canonical names defined in two places that could silently drift apart. Fixed by importing from `graphData.ts` instead.
    3. **Important:** `ui-rules.md`'s own written "1–3 small looping elements per section" rule was stale against `PlatformGraph`'s actual ~12 concurrent loops. Rather than silently leaving the doc wrong or stripping the motion the user explicitly asked for, added a **Confirmed exception** entry directly under that rule in `ui-rules.md`, naming `PlatformGraph` specifically and the reasoning (explicit, repeated user request for this one hero visual, not license to add more loops elsewhere by default).
    4. **Minor:** the decorative `<svg>` in `PlatformGraph.tsx` had no `aria-hidden`. Added it — its entire contents (lines, dots) are connecting-line decoration, not real content (the actual topic/collection names remain in plain accessible text elsewhere in the same component).
    5. **Minor:** `DOT_FILL_CLASSES[index]` would have silently returned `undefined` (no fill class, falling back to SVG's default black fill) if a 5th topic were ever added to `LEFT_TOPICS`/`RIGHT_TOPICS` without also extending `DOT_FILL_CLASSES`/`Y_SLOTS`. Fixed with modulo indexing (`DOT_FILL_CLASSES[index % DOT_FILL_CLASSES.length]`) so the 3 independently-sized arrays can never produce an out-of-bounds lookup, even though they're not type-linked.
    6. **Minor, flagged as pre-existing/systemic but fixed in scope for this feature anyway:** no `prefers-reduced-motion` handling existed anywhere in the codebase, and this feature meaningfully raised how much continuous motion runs on the homepage. Rather than building a global solution (out of scope for one feature's review), added `useReducedMotion()` (from `framer-motion`) to every component in this feature that runs a continuous loop — `GraphLine`, `CardBorderGlow`, and `PlatformGraph`'s pulse/sparkle — so each collapses to a single static frame (still visible, just not animating) when the OS-level reduced-motion preference is on, instead of looping regardless. Verified via a Playwright context with `reducedMotion: 'reduce'`: screenshot confirms the dots render as static (non-moving) accents, and the only console output is Framer Motion's own informational notice that it detected the preference — not an error.
    - Re-verified after all 6 fixes: typecheck/lint clean; full console check clean in both normal and reduced-motion modes; Playwright screenshots desktop/mobile × light/dark unchanged from the last known-good state (no visual regression from the fixes); no new overflow.
  - Pushed `feature/06-feature-highlights-section` to origin and opened the PR → `develop`; merged via PR #9. Local `develop` fast-forwarded to the merge commit (`15a34f5`).
**Last completed:** 07 Testimonials Section — ran the `architect` skill (manually) before building. No design screenshot existed in `designs/` for this section (only hero variants + Learn page), but the user supplied a reference screenshot of GreatFrontEnd's actual testimonials section partway through the architect session — built to match that structure (true masonry: 3 independent columns, "Offer from {Company}" header with a company logo + date, quote, avatar + name + role/location footer) with 100% original Frontend Forever content, not copied branding/people/photos. Two decisions resolved with the user up front: (1) cards stay neutral white (`bg-surface`, `ui-rules.md`'s standard treatment), not the 5-hue per-card theming used elsewhere on the homepage — confirmed by the user as more credible for social-proof content; color instead shows up only at the avatar level (initials tinted via the same 5-hue token system, one new use of the previously-unused shadcn `Avatar`/`AvatarFallback` primitive). (2) Heading follows the reference's stat-led, two-tone pattern ("500+ developers" dark / "who get it now." gray) with an explicit placeholder number, rather than build-plan's literal plain "Developers who get it now" — user explicitly chose to match the reference despite the number being invented; left-aligned and sized larger than this codebase's usual centered `text-2xl/3xl` section heading (`HowItWorksSection`/`FeatureHighlightsSection`), a deliberate one-off deviation for this section only.
  - New files: `src/components/homepage/TestimonialsSection.tsx` (splits `TESTIMONIALS` into 3 column-arrays via `index % 3`, renders the masonry grid + heading) and `src/components/homepage/testimonials/{testimonialsData.ts,TestimonialCard.tsx}`.
  - Real decision during build, not just style: the company "logo" badges reuse the existing `shared/logos/` components from Feature 04 (Google, Meta, Amazon, Microsoft, Stripe, Anthropic — 6 of the existing 7) rather than sourcing new square app-icon-style marks like the reference's Apple/Shopify/Adobe/Airbnb icons. Considered wrapping each logo in a small circular badge to visually match the reference, but `MetaLogo`/`MicrosoftLogo` are icon+wordmark lockups (not square glyphs) that would squish illegibly inside a fixed circle — dropped the circular-badge idea and rendered each logo inline at a normalized `h-4` next to the "Offer from {Company}" text instead (Meta/Microsoft don't actually shrink to `h-4` since their own internal text span is independently sized — same already-documented, accepted height-variance precedent as `CompanyLogosStrip`). This also ties the testimonials' "got hired at" stories back to the same 6 companies already named in Feature 04's logo strip.
  - Quote content is original and deliberately specific per testimonial — names a different real platform feature each time (Event Loop simulator + FF System Design, Build tab + Kanban project, spaced repetition review queue, React Rendering simulator, Challenge tab virtualized-list problem, FF 75 + Browser Pipeline simulator) rather than generic praise, per the same "be specific, not generic" lesson learned in Feature 06. 6 fictional people across 6 countries, varying quote lengths (feeding the masonry effect), 2 of the 6 cards include an "Other offers:" line as an authenticity touch from the reference (not on every card, matching how the reference itself only showed it once among the visible cards).
  - Verified: typecheck (`npx tsc --noEmit`) and lint (`npx eslint`) clean; Playwright screenshots at desktop (1440px) and mobile (390px) × light/dark confirmed the masonry column-height variation, two-tone heading, per-avatar color variety, and correct single-column mobile stacking; confirmed via `getBoundingClientRect()`-based scroll positioning (not an element screenshot, which produces a known sticky-header compositing artifact on elements taller than the viewport) that the real navbar does not overlap the section; mobile overflow check unchanged from the pre-existing `CompanyLogosStrip` baseline (`scrollWidth` 440 vs `clientWidth` 390).
  - **Follow-up (same session, before any PR):** user asked for 6 more testimonials (12 total) and to swap the heading from the no-stat "Developers who get it now" wording to the reference's literal stat-led pattern with a different placeholder number: "1M+ developers trust us" (dark) / "with their job interviews." (gray). Added 6 more fictional testimonials, each naming a still-different specific platform feature not already covered by the first 6 (FF JavaScript collection, CSS Specificity simulator, Practice's debounce/throttle challenge, FF React's reconciliation deep dive, a Build-tab component-library project, the daily streak system) — same "specific, not generic" approach as the first batch. The 7th company logo (`CursorLogo`, unused in the first batch) was used once; the other 5 new entries reuse companies already shown in the first 6 (Google, Stripe, Amazon, Meta, Microsoft) with different fictional people — confirmed via `index % 3` column distribution that this still produces a clean 4/4/4 masonry split with no layout regression. `--avatarColor` cycling and the optional `otherOffer` field (now on 3 of 12 cards) both still work unchanged — `testimonialsData.ts`'s structure didn't need to change, only its content grew.
  - Re-verified after the follow-up: typecheck/lint clean; DOM-counted (not just screenshotted) that all 12 cards render with an even 4/4/4 column split; Playwright screenshots desktop/dark confirming the new heading and full 12-card layout; mobile overflow check unchanged (`scrollWidth` 440 vs `clientWidth` 390, same pre-existing `CompanyLogosStrip` baseline).
  - **Follow-up 2 (real avatar photos, same session, before any PR):** discussed adding profile photos — flagged the honesty tradeoff up front (fictional testimonials with stock/AI photos can read as a dark pattern if scrutinized) and offered generated-illustration avatars as the lower-risk default; user chose to supply real image files instead. Viewed 2 of the 12 supplied photos standalone before wiring any of them in (same "verify a supplied asset before trusting it" precedent as the garbled Meta SVG caught in Feature 04) — confirmed they're plausible AI-generated headshots, not real people's actual photos, consistent with this being fictional content. Files placed at `public/avatars/testimonials/{id}.jpg`, named to match each testimonial's existing `id` field so no new data field was needed — `TestimonialCard.tsx` computes the path directly from `id`. Added `AvatarImage` (base-ui, via the shadcn `Avatar` primitive) alongside the existing `AvatarFallback`, which now only renders if a given photo is missing or fails to load.
  - Re-verified: typecheck/lint clean; zero browser console errors confirmed via Playwright across all 12 cards; screenshots desktop/dark confirming every photo renders correctly, cropped circular, with no contrast issues against either theme's card background; mobile overflow unchanged.
  - **Follow-up 3 (`/review`, then fixed both findings, same session, before any PR):** ran the project's `review` skill manually against the finished feature. Found 0 critical/important issues and 2 minor ones, both fixed:
    1. **Confirmed-correct but unverified fallback path:** read the installed `@base-ui/react` Avatar source directly (`useImageLoadingStatus.js`, `AvatarFallback.js`) rather than assuming — confirmed `AvatarFallback` is `enabled` whenever `imageLoadingStatus !== 'loaded'`, including on `image.onerror`, so a missing/broken photo file correctly falls back to initials. This was previously only assumed from the primitive's typical behavior, not actually checked; now confirmed by reading the library source. No code change needed, just closed the verification gap.
    2. **Unoptimized avatar photos:** the 12 user-supplied JPEGs were 674–824px square (18–20KB each, ~230KB total) being displayed at a 40px `Avatar size="lg"` — way oversized for their actual display size, the same class of issue as the oversized hand-drawn icon SVGs caught in Feature 05 (`npx svgo --precision 2`, an 86–92% reduction). `sharp` was already available via Next's own dependency tree (confirmed via `require.resolve`), so resized + recompressed all 12 in place to 160×160 (cover-cropped) at JPEG quality 82 — enough headroom for 4x display density at 40px. Result: ~230KB → ~66KB total (71% reduction), verified via a re-screenshot that nothing visibly changed at actual display size. Also added a one-line comment in `TestimonialCard.tsx` documenting the implicit `testimonial.id` ↔ `public/avatars/testimonials/{id}.jpg` filename contract, since nothing enforces it at compile time and a future testimonial added without a matching photo would silently rely on the fallback path above.
    - Re-verified after both fixes: typecheck/lint clean; zero console errors; mobile overflow unchanged; screenshot confirms no visual regression from the recompression.
  - Pushed `feature/07-testimonials-section` to origin; PR → `develop` merged via PR #11. Local `develop` fast-forwarded to the merge commit (`1bbfaa6`).
**Last completed:** 08 CTA Section — ran the `architect` skill before building. Build-plan's spec ("dark background section — `bg-accent-dark` or rich dark with teal accent") was an open decision rather than a settled term; the user asked for creative direction matching the platform's existing brand rather than picking between the two literal options. Built: single full-bleed closing section ("Start learning for free" + supporting subtext + one "Get Started" → `/login` button), `bg-accent-darker` (the existing token, not a new color), 2 restrained decorative glow blobs, one-shot `whileInView` scale+fade on the section with a staggered fade-up underneath (badge → heading → subtext → button) — no continuous/ambient loops, since this is meant to read as a clean one-shot close, not another feature showcase.
  - New file: `src/components/homepage/CTASection.tsx`. Mounted as the last top-level sibling in `src/app/page.tsx`, after `TestimonialsSection` (Footer is layout-level, in `src/app/layout.tsx`, not part of this page-level list).
  - **Real bug caught and fixed before any verification, not after:** the first draft used `text-inverse` for heading/subtext and `bg-surface` for the button. Both are wrong for this component specifically — `bg-accent-darker` (this section's background) stays a dark teal in *both* light and dark theme, but `text-inverse`/`bg-surface` both flip polarity *with* the page theme (inverse: white→near-black, surface: white→near-black) rather than staying a fixed color — which would have made the heading/subtext nearly invisible against the still-dark background once dark mode was toggled. This is the exact same class of token-misuse bug already caught once in Feature 06 (`ProjectEditorMiniVisual`'s dark-mode text), caught here before writing any further code rather than after a screenshot revealed it. Fixed by using `text-accent-foreground`/`bg-accent-foreground` instead — `--color-accent-foreground` (`#FFFFFF`) is the one color token in `ui-tokens.md` that is *not* redefined inside the `.dark {}` block, so it stays white in both themes by design (it exists specifically for "foreground text on a colored surface," which is exactly this case). Verified via computed-style checks (not just a screenshot) that the heading renders `rgb(255,255,255)` in both themes before moving on.
  - **Verification artifact, diagnosed rather than assumed to be a real bug:** an automated check that jumped straight to this section via `element.scrollIntoView({ behavior: "instant" })` from page-load left it stuck at `opacity: 0`, which looked like a broken `whileInView` trigger at first. Reproduced the exact same instant-jump method against the already-shipped, working `TestimonialsSection` and got the identical stuck result — confirming this is a generic headless-browser/IntersectionObserver artifact of one large instant jump with no intermediate scroll frames, not a defect in this feature's code. Re-verified by scrolling through all 5 homepage sections sequentially with real pauses between each (matching how an actual user scrolls) — every section, including this new one, animates to `opacity: 1` correctly.
  - Verified: typecheck (`npx tsc --noEmit`) and lint (`npx eslint`) clean; Playwright screenshots at desktop (1440px) and mobile (390px) × light/dark confirming the full-bleed band, badge/heading/subtext/button contrast staying correct in both themes, and the button's `/login` href; mobile overflow check unchanged from the pre-existing `CompanyLogosStrip` baseline (`scrollWidth` 719 vs `clientWidth` 390 in this run — within the already-documented 440/510/627/719 fluctuation range, still traced to that same pre-existing element, not a new regression).
  - **Follow-up (background swap, same session, before any PR):** user didn't like the flat solid-teal (`bg-accent-darker`) background. Asked for a concrete direction rather than re-opening the original open-ended question; offered 3 options (rich near-black with teal as accent only / a teal-to-near-black gradient / drop the "dark section" idea entirely) — user picked the near-black-with-teal-accent direction, closer to Stripe/Linear's dark closing CTA bands.
    - Added a new token, `--color-cta-dark` (`#0A0A0A`), to `globals.css`/`ui-tokens.md` — deliberately **not** redefined inside `.dark {}`, the same theme-invariant precedent already established by `--color-accent-foreground`, since this section needs to look identically dark in both site themes rather than flip polarity with the page the way `bg-background`/`bg-surface` do. Documented as a new section in `ui-tokens.md` ("Theme-Invariant Surfaces") and cross-referenced from `ui-rules.md`'s Dark Mode section alongside the existing Monaco-editor exception — this is now the second deliberate "always the same regardless of theme" surface in the codebase.
    - Swapped `CTASection`'s background from `bg-accent-darker` to `bg-cta-dark`, pulled teal back to an accent role only: a 1px top-edge gradient line (`from-transparent via-accent to-transparent`) and 2 blurred `bg-accent` glow circles sit on top of the near-black surface, rather than the surface itself being teal. The button kept its existing `bg-accent-foreground`/`text-accent-darker` treatment unchanged.
    - **Confirmed the earlier theme-invariant-foreground fix paid off immediately:** because the heading/subtext/badge already used `text-accent-foreground` (not `text-inverse`) from the bug caught during the original build, swapping the background token required zero text-color changes — the foreground was already theme-invariant, so it stayed correct against the new background automatically. This is the generalization the original fix was meant to produce, confirmed by reuse on the very next change rather than by inspection alone.
    - Re-verified: typecheck/lint clean; Playwright computed-style checks confirming the section background renders `rgb(10, 10, 10)` and heading/button colors stay unchanged across all 4 theme/viewport combinations; screenshots desktop/mobile × light/dark confirming the near-black band with glow accents reads as intended.
  - **Follow-up (`/review`, same session, before any PR):** ran the project's `review` skill against the finished feature. Found 1 minor issue, no critical/important ones: the "Free to start, no credit card required" badge pill above the heading was added during the build but never called out as a deviation from the architect-aligned plan (which only specified badge-less heading + subtext + button). Flagged to the user as a judgment call rather than fixed unilaterally — user accepted the feature as-is and merged it, so this stands as a confirmed, accepted addition, not outstanding debt.
  - Pushed `feature/08-cta-section` to origin; PR → `develop` merged via PR #13. Local `develop` fast-forwarded to the merge commit (`1617af4`).
**Last completed:** 09 Event Loop Simulator (Hero Version) — ran the `architect` skill before building. Resolved language/decisions up front with the user: "Hero version" means a single hardcoded scenario with the full standard control set (Play/Step/Step Back/Restart/Autoplay/Speed) — Feature 23 later adds scenario *switching* on top of the same engine, not more controls. Kept `BrowserChromeBar`/`PanelHeader` inside this feature's own `components/` rather than promoting them to `src/components/shared/` (rule-of-three — Features 10–12 will likely want the same chrome, but extracting for one current consumer would be premature). Replaced the static `HeroSimulatorPreview.tsx` mock entirely — `Hero.tsx` now renders the real `EventLoopSimulator` from `src/features/simulators/event-loop/`.
  - New self-contained feature module: `src/features/simulators/event-loop/` — `types.ts`, `data/scenarios.ts` (`EVENT_LOOP_FRAMES`, `EXECUTION_ORDER_BY_OUTPUT`), `hooks/useEventLoopSimulator.ts`, and `components/{EventLoopSimulator,BrowserChromeBar,PanelHeader,SimulatorPanel,CallStack,WebAPIs,MicrotaskQueue,TaskQueue,CodePanel,ExecutionOrder,SimulatorControls}.tsx`. Matches `architecture.md`'s explicit per-simulator file list (one file each for CallStack/WebAPIs/MicrotaskQueue/TaskQueue), implemented as thin themed wrappers around one shared internal `SimulatorPanel.tsx` rather than 4x-duplicated JSX — sharing within a single simulator's own folder doesn't violate the "no shared simulator engine across simulators" invariant.
  - **Real design decision, not diff-action replay:** `scenarios.ts` authors each of the 8 steps as a complete resultant panel snapshot (`callStack`/`webAPIs`/`microtaskQueue`/`taskQueue`/`consoleOutput`/`activeCodeLines`), and the hook just indexes `EVENT_LOOP_FRAMES[currentStep - 1]` — no runtime folding/replay of push/move/output actions. This makes Step Back trivial (decrement index, recompute nothing) and is the most literal possible compliance with the architecture invariant "step scripts are the source of truth, never derive steps from runtime computation."
  - **Play vs. Autoplay are genuinely separate controls**, per the build-plan's state shape listing both `isPlaying` and `autoplay` independently: Play runs the 8-step sequence through once and stops at the end; Autoplay (a `Switch`) makes it loop back to step 1 and keep going once Play is engaged. Verified via Playwright that toggling Autoplay before/while at step 8 and clicking Play correctly wraps back to step 1 and continues, while Play alone (autoplay off) stops cleanly at step 8.
  - **Mobile simplification required zero extra code:** per `ui-rules.md`'s "Hero simulator is simplified (no interactive controls) on mobile," the engine's default state (`currentStep: 1`, `isPlaying: false`) is already exactly "frozen at step 1" — hiding `SimulatorControls` via a plain `hidden md:block` wrapper was sufficient on its own, no `useMediaQuery` hook or mobile-specific component needed, since nothing can trigger a step change without the controls being visible.
  - **Verification caught a false alarm, not a real bug:** an early Playwright screenshot of the homepage top looked completely blank below the navbar (no headline, no simulator) after only an 800ms wait. Re-checked with `waitUntil: "networkidle"` + a 2.5s wait and it rendered correctly — confirmed as a first-hit Next.js dev cold-compile delay on the freshly-edited `Hero.tsx` route, not a runtime defect (no console/page errors either time).
  - **Verification method for per-step code highlighting:** the `bg-accent-muted` highlight color (`#F0FDFA`) is subtle enough that a compressed screenshot crop genuinely couldn't be read by eye with confidence about exactly which code line was highlighted at a glance. Used a direct `getComputedStyle().backgroundColor` check on every code line across all 8 steps instead of trusting the screenshot — confirmed the highlighted line(s) exactly match `scenarios.ts`'s authored `activeCodeLines` for every frame, same "measure, don't eyeball" precedent as `getBoundingClientRect()` checks used elsewhere in this project (Feature 04 logo sizing, Feature 06 `PlatformGraph`).
  - Verified: typecheck (`npx tsc --noEmit`) and lint (`npx eslint`) clean; Playwright across desktop (1440px) × light/dark confirmed Step/Step Back/Restart/Play/Pause/Autoplay-loop/Speed all transition to the correct frame (DOM text assertions, not just screenshots); mobile (390px) confirmed frozen at "Step 1 of 8" with controls hidden and no new overflow beyond the pre-existing `CompanyLogosStrip` baseline (`scrollWidth` 440 vs `clientWidth` 390); zero console/page errors in any check.
  - **Follow-up (4 fixes, same session, before any PR):** user reviewed screenshots and reported 4 issues — fixed all 4, 3 real component bugs plus 1 environment artifact:
    1. **`BrowserChromeBar`'s address pill wasn't horizontally centered** in the chrome bar — root cause: the bar was a plain `flex` row (traffic-lights, then a `flex-1 justify-center` div for the pill), which centers the pill within the *remaining* space after the traffic-lights' width, not within the bar's true full width, so the pill always sat visibly left of center. Fixed by switching to `grid grid-cols-[1fr_auto_1fr]` — traffic lights in the first (left-aligned) column, the pill auto-sized in the middle column, and an empty `aria-hidden` spacer in the third column so both outer columns absorb equal leftover space. Verified via `getBoundingClientRect()` that the pill's center now exactly equals the bar's center (0.0px diff), not just visually.
    2. **No literal "output" card existed** — only the Execution Order bar implied what had run. Split the previous full-width `CodePanel` row into a 2-column row using the same 4-column grid template as the panels above: `CodePanel` now `lg:col-span-3` (aligned under Call Stack/Web APIs/Microtask Queue) and a new `OutputPanel.tsx` (`lg:col-span-1`, aligned under Task Queue) — a small terminal-style card listing `consoleOutput` lines (`> start`, `> end`, ...) colored by the same per-origin theme already used by `ExecutionOrder`. Verified via DOM `closest('.rounded-lg')` measurement (not a screenshot guess) that Code's left edge and width exactly match Call Stack→Microtask Queue's combined span, and Output's exactly match Task Queue's span.
    3. **Active code-line highlight (`bg-accent-muted`) was too subtle to read as "currently executing."** Replaced with a more deliberate "current line" treatment, by request to use creative judgment: a left accent border (`border-l-2 border-accent`), a stronger `bg-accent/10` fill, and a small reused "live" ping-dot (the same ping pattern already established in `PanelHeader`'s live indicator) in a fixed-width gutter before each line, so only the active line gets the dot and no lines shift horizontally when the active line changes.
    4. **`CTASection` appeared completely unstyled (white background, invisible white-on-white text) in the user's screenshot**, looking like a regression. Investigated rather than assumed: confirmed via `git show --stat` that Feature 09's commit never touched `CTASection.tsx` or `globals.css`, and confirmed `--color-cta-dark` was still correctly defined inside `@theme {}`. Checked the live computed background-color and got `rgba(0,0,0,0)` (transparent) instead of the expected near-black — a real serving bug, not a misread screenshot. Diagnosed as the same stale-Turbopack-dev-cache class of issue already documented once in Feature 01 (`globals.css` changes not reflected after enough hot-reload churn across a long session). Fixed the same way: killed the long-running dev server, deleted `.next`, restarted clean — re-checked computed background and got the correct `rgb(10, 10, 10)`. Not a code defect; no source file changed for this one.
    - Re-verified all 4 fixes together: typecheck/lint clean; Playwright desktop × light/dark confirmed the centered chrome pill, the aligned Code/Output split, the more visible active-line indicator (both themes), and the restored CTA section; mobile re-confirmed still frozen at "Step 1 of 8" with controls hidden and no new overflow.
  - **Follow-up 2 (real correctness bug, same session, before any PR):** user caught that frames 7 and 8 showed the Promise `.then` and `setTimeout` callbacks "executing" directly inside the Microtask Queue / Task Queue panels, with the Call Stack left empty — explicitly asked me to confirm I understood the actual JS event-loop semantics before touching the fix. Confirmed and explained back: queues only ever hold a *waiting* callback; the event loop must dequeue it onto the Call Stack before it runs, since the Call Stack is the only place code executes — showing "Executing..." inside a queue slot teaches the architecture backwards. Fixed in `scenarios.ts`: frame 7 now empties `microtaskQueue` and puts `{ code: "console.log('promise')", status: "Executing..." }` in `callStack`; frame 8 does the same for `taskQueue` → `callStack` with `console.log('timeout')` — matching frames 1/2/5/6, which already only ever show "Executing..." inside the Call Stack panel.
    - Flagged but deliberately left alone (out of scope for what was reported): the Promise `.then` registration still shows up in the "Web APIs" panel alongside the real `setTimeout` timer (steps 3–6), inherited from the original Feature 03 static mock — real JS never routes promise microtasks through Web APIs (only the timer is a genuine Web API; a resolved promise's callback should sit in the Microtask Queue from the moment it's registered). Noted to the user as a separate, bigger design call; not changed without being asked.
    - Verified the fix itself via direct DOM text inspection of each panel (not a screenshot) across all 8 steps. Hit a confusing false read on the first pass — stepping forward with only 150ms between clicks showed step 6's Call Stack still containing step 5's item and step 7's Web APIs still containing the already-removed Promise item — but this was the test script outrunning the panels' own 300ms exit animation, not a real bug: re-running with 600ms waits (longer than the transition) showed every panel settle to the exact authored state, and a second pass with the browser's network cache fully disabled (`Network.setCacheDisabled` via CDP) on a fresh navigation confirmed the same correct result independent of any caching variable.
  - **Follow-up 3 (full event-loop accuracy rewrite, same session, before any PR):** user pushed back again on Follow-up 2's fix — having both callbacks jump straight from Web APIs to the Call Stack defeats the entire purpose of the Microtask Queue / Task Queue panels existing at all. User then provided a complete, explicit 11-step real-world execution trace (Web APIs → queue → Call Stack for both callbacks, microtask queue fully drained before the task queue is ever checked) and asked for confirmation of understanding before any further fix — same "let me know before fixing" gate as Follow-up 2. Confirmed back, including two corrections beyond the original 8-step scenario that the user's trace made explicit: (1) `setTimeout`'s callback enters the Task Queue as soon as its 0ms timer fires, independent of whether the synchronous script is still running — not only after the Call Stack empties; (2) `Promise.resolve().then(...)` in this scenario does **not** genuinely involve a Web API in real JS (it resolves synchronously) — the user's trace has it going straight to the Microtask Queue, which is more accurate than the original static mock's choice (inherited from Feature 03) to show it sitting in Web APIs alongside the timer. Flagged one deliberate non-change: no explicit "Global Execution Context" Call Stack frame, since the simulator's Call Stack panel has always only ever shown real user-code statements, never engine-internal bookkeeping frames — kept that abstraction rather than adding new fidelity nobody asked for.
    - **Immediate follow-up before implementing:** user added one more deliberate, explicit deviation from real `Promise.resolve()` semantics — since real frontend code almost always uses `fetch()` (a genuine Web API) rather than a synchronously-resolving `Promise.resolve()`, the user asked for the Promise's path to also detour through Web APIs first (mirroring `setTimeout`'s shape exactly: Call Stack → Web APIs → its queue → Call Stack), treating the existing `Promise.resolve().then(...)` code text as a deliberate stand-in for an async call like `fetch`. Did not rename the displayed source code to `fetch(...)` — not explicitly requested, and the existing code text + a distinct Web API status (`"Pending..."`, not reusing `setTimeout`'s `"Timer started"`) communicates the same lesson without changing the literal code shown in the panel.
    - Rewrote `scenarios.ts` from 8 frames to a fully event-loop-accurate **12 frames** (`TOTAL_STEPS` is just `EVENT_LOOP_FRAMES.length`, so "Step X of 12" updates with zero changes to the hook or any component): `setTimeout` now flows Call Stack (register) → Web APIs (`"Timer started"`) → Task Queue (`"Waiting..."`, queued the instant the timer fires, several frames before the Call Stack ever empties) → Call Stack (`"Executing..."`) → empty. `Promise.resolve().then(...)` now flows the identical shape — Call Stack (register) → Web APIs (`"Pending..."`) → Microtask Queue (`"Waiting..."`) → Call Stack (`"Executing..."`) → empty — rather than skipping Web APIs. Both queues sit populated simultaneously for several frames (steps 6–8) before either drains, and the Microtask Queue is fully emptied (step 9) before the Task Queue is touched at all (step 11) — the actual event-loop priority rule the whole feature exists to teach.
    - Re-verified the complete 12-frame sequence via direct DOM text inspection of all 4 panels at every single step (not a screenshot), with the browser's cache disabled on a fresh navigation. Hit the same false-read pattern as Follow-up 2 once more — a 500ms-per-step wait showed step 11's Task Queue one frame stale (still showing the dequeued item) — re-ran with 1000ms waits and got the exact authored state at every step, confirming it was the same render/exit-animation timing artifact, not a logic bug. Re-ran typecheck/lint clean after the rewrite.
  - **Follow-up 4 (`/review`, then fixed everything found, same session, before any PR):** ran the project's `review` skill manually against the finished feature. Found 8 issues (0 critical, 3 important, 5 minor) across all 3 layers — all fixed:
    1. **Important** — `useEventLoopSimulator()` had no explicit return type, against `code-standards.md`. Added a named `UseEventLoopSimulatorResult` type.
    2. **Important** — `CallStack`/`WebAPIs`/`MicrotaskQueue`/`TaskQueue` had no empty-state treatment (just blank space) when `items` was `[]`, against `ui-rules.md`'s "never skip empty states" rule — and these panels sit empty for most of the 12 steps. Added a required `emptyLabel` prop to `SimulatorPanel`, with each wrapper passing contextual text ("Stack is empty", "No active requests", "Queue is empty" ×2).
    3. **Important** — no `aria-live` region anywhere, so a screen-reader user pressing Play/Step got zero announcement that anything changed. Added a `description` field to every `SimulatorFrame` (a plain-English sentence per step) and a `sr-only role="status" aria-live="polite"` span in `EventLoopSimulator.tsx` announcing `"Step X of Y: {description}"`.
    4. **Minor** — decorative elements lacked `aria-hidden`: `BrowserChromeBar`'s traffic-light dots + fake address text, and `CodePanel`'s gutter ping-dot. Added `aria-hidden="true"` to both.
    5. **Minor** — the live-indicator ping dots (`PanelHeader`, `CodePanel`) used raw Tailwind `animate-ping`, which doesn't respect `prefers-reduced-motion`. Added a guard so the ping span is omitted (leaving just the static dot) when reduced motion is preferred, in `PanelHeader`, `CodePanel`, and the new Call Stack pulse (next item).
    6. **Minor** — `ExecutionOrder`'s labels (`"Promise.then(...)"`/`"setTimeout(...)"`) didn't match what Call Stack actually showed while executing (`"console.log('promise')"`/`"console.log('timeout')"`). Aligned `EXECUTION_ORDER_BY_OUTPUT` in `scenarios.ts` to the executing-time labels.
    7. **Minor** (Layer 1 gap) — build-plan's "Active item in CallStack has pulse highlight" wasn't distinctly implemented — every panel's dot looked identical. Added a `pulse` prop to `SimulatorPanel` (only `CallStack` sets it), rendering the same ping-style dot already used elsewhere instead of a plain static one, for Call Stack items specifically.
    8. Two Layer 1 deviations from `architecture.md`'s literal documented shape (a single `frame` object instead of separate `callStack`/`webAPIs`/etc. state fields; `OutputPanel.tsx` instead of the documented `ConsoleOutput.tsx` name) were reviewed and left as-is — both are deliberate, reasonable design choices already covered by this entry's own documentation, not defects to fix.
    - **Real bug caught while fixing #5, not by the review itself:** calling Framer Motion's `useReducedMotion()` directly inside `PanelHeader`/`CodePanel`/`SimulatorPanel` (all part of the server-rendered tree) caused a genuine hydration mismatch — confirmed via a Playwright context with `reducedMotion: 'reduce'`, which threw a real React hydration-mismatch error and a "script tag" warning. Root cause: the hook can resolve the real media-query value synchronously on the client's very first render (before hydration completes), which doesn't match the server's unconditional `false`. This is the same root-cause shape already solved once in this codebase for `ThemeProvider` (see Decisions Made → 01 Design System) — fixed the same way: added `hooks/useSafeReducedMotion.ts` using `useSyncExternalStore` (`getServerSnapshot` always `false`, `getSnapshot` the real `window.matchMedia` check) instead of a `useState`+`useEffect` "has mounted" flag, which also avoids the `react-hooks/set-state-in-effect` lint error a naive fix would have hit (confirmed by hitting that exact lint error on the first attempt before switching to `useSyncExternalStore`). Re-verified with the same reduced-motion Playwright context afterward: zero hydration errors, ping dots correctly absent, plain dots correctly present.
    - Also caught and fixed a performance slip introduced by the same fix: `useReducedMotion()`/`useSafeReducedMotion()` was initially called once per `CodeLine` (11 times per render, one per code line) instead of once in the parent `CodePanel` and passed down — fixed before it ever shipped.
    - Re-verified everything together: typecheck/lint clean; Playwright confirmed all 4 panels' empty states, the aria-live text per step, the Call Stack pulse (present normally, absent under reduced motion, with zero hydration errors), and the aligned Execution Order labels — both via direct DOM/computed-style checks and screenshots, desktop/mobile × light/dark; re-ran the full 12-step regression check with cache disabled and got byte-for-byte the same correct sequence as before these fixes. One dark-mode screenshot via a `page.reload()`-based test came back fully blank below the navbar despite correct DOM/colors/opacity underneath (confirmed via direct computed-style inspection) — re-tested with a single `page.goto()` + `addInitScript()` instead of a `reload()` and got a correct screenshot immediately, isolating it to a `reload()`-specific Playwright/dev-server timing quirk in this long-running session, not an application defect.
  - Pushed `feature/09-event-loop-simulator` to origin (not yet merged — PR opened and being managed manually by the user).
  - **Follow-up 5 (CodeRabbit PR review, same session):** user pasted CodeRabbit's automated findings from the open PR. Verified each against the current code before acting, per the project's CodeRabbit-findings-must-be-addressed rule — fixed 2 real issues, skipped 1 that wasn't actually a defect:
    1. **Skipped — not a bug.** CodeRabbit flagged `hidden md:block` on `SimulatorControls` as hiding all playback controls on mobile. Confirmed this is deliberate: it directly implements `ui-rules.md`'s explicit "Hero simulator is simplified (no interactive controls) on mobile" rule, decided with the user during this feature's own architect session and already documented in this entry and `ui-registry.md`. CodeRabbit has no access to that project context.
    2. **Fixed — real type-safety gap.** `ExecutionOrder`/`OutputPanel` typed `consoleOutput` as `string[]` and indexed `EXECUTION_ORDER_BY_OUTPUT[output]` without a defined-check — sound today only because `scenarios.ts`'s own data happens to be internally consistent, but nothing enforced that. Added `CONSOLE_OUTPUT_TOKENS`/`ConsoleOutputToken` (a closed `"start" | "end" | "promise" | "timeout"` union) to `types.ts`, retyped `SimulatorFrame.consoleOutput` and both components' props to use it, and retyped `EXECUTION_ORDER_BY_OUTPUT` as `Record<ConsoleOutputToken, ExecutionOrderEntry>` (exhaustive, not `Record<string, ...>`) — the lookup is now sound by construction; a typo'd or future invalid output token fails at compile time instead of risking a runtime crash.
    3. **Fixed — real UX bug.** `PanelHeader`'s "Running" badge was hardcoded green text regardless of actual play state — inherited unchanged from the original static mock, where it was just decorative flavor text since nothing was real yet. Now that the engine has genuine play/pause state (default: paused, frozen at step 1), the hardcoded label was actively wrong most of the time. Added an `isPlaying` prop; badge now reads "Paused" (`bg-surface-secondary text-text-secondary`, muted dot) when not playing and "Running" (existing green treatment) when playing.
    - Re-verified: typecheck/lint clean; Playwright confirmed the badge text and color swap correctly on Play/Pause (text assertions, not just a screenshot), and re-ran the full 12-step DOM regression to confirm the type changes didn't alter any runtime behavior — byte-for-byte the same correct sequence as before.
**Currently building:** Nothing in progress.
**Next:** Start 10 React Rendering Simulator (Hero Version)

---

## Progress

### Phase 0 — Foundation

- [x] 00 Project Setup
- [x] 01 Design System + Global Styles
- [x] 02 Navbar + Footer

### Phase 1 — Homepage

- [x] 03 Hero Section (Static)
- [x] 04 Company Logos Strip
- [x] 05 How It Works Section
- [x] 06 Feature Highlights Section
- [x] 07 Testimonials Section
- [x] 08 CTA Section
- [x] 09 Event Loop Simulator (Hero Version)
- [ ] 10 React Rendering Simulator (Hero Version)
- [ ] 11 Browser Pipeline Simulator (Hero Version)
- [ ] 12 CSS Specificity Simulator (Hero Version)
- [ ] 13 Concept Switcher Wiring

### Phase 2 — Auth

- [ ] 14 Supabase Setup
- [ ] 15 Login Page
- [ ] 16 Auth Callback Handler
- [ ] 17 Logged-In Navbar Variant

### Phase 3 — Database

- [ ] 18 Supabase Tables + RLS
- [ ] 19 Upstash Redis Setup

### Phase 4 — Learn Experience

- [ ] 20 Learn Index Page
- [ ] 21 Concept Page Shell
- [ ] 22 Understand Tab
- [ ] 23 Simulate Tab — Full Simulators
- [ ] 24 Challenge Tab
- [ ] 25 Interview Tab
- [ ] 26 Build Tab
- [ ] 27 Progress API + XP System

### Phase 5 — Practice Section

- [ ] 28 Practice List Page
- [ ] 29 Challenge Editor Page

### Phase 6 — Interview Prep

- [ ] 30 Interview Prep Hub
- [ ] 31 Collection Pages (FF 75, FF JS, FF React)
- [ ] 32 Spaced Repetition Review Session

### Phase 7 — Explore + Roadmaps

- [ ] 33 Explore Page
- [ ] 34 Roadmaps List Page
- [ ] 35 Roadmap Detail Page

### Phase 8 — Leaderboard + Gamification

- [ ] 36 Leaderboard Page
- [ ] 37 Streak System Polish

### Phase 9 — Premium + Stripe

- [ ] 38 Premium Content Gating
- [ ] 39 Stripe Integration

---

## Decisions Made During Build

- **00 Project Setup:** `create-next-app@latest` installed **Next.js 16.2.9** (React 19.2), not Next 15 as named in `architecture.md`. Followed the build-plan's literal `@latest` instruction rather than pinning to 15. Stack decision in `architecture.md` should be read as "Next.js 16" going forward.
  - Breaking change to plan for in **Feature 14 (Supabase Setup)**: Next 16 renames `middleware.ts`/`middleware()` to `proxy.ts`/`proxy()`. The `edge` runtime is not supported in `proxy` (runtime is fixed to `nodejs`). `architecture.md` and `build-plan.md` still say `middleware.ts` — use `proxy.ts` instead when implementing session validation.
  - Breaking change to plan for across all dynamic routes: `params`/`searchParams` (and `cookies()`/`headers()`) are async-only in Next 16 — always `await` them, no synchronous fallback.
  - Turbopack is on by default in 16; `next.config.ts` sets `turbopack.root` explicitly because a stray `package-lock.json` exists at `C:\Users\DEEPSHIKHA\` (outside this repo) which Next's workspace-root inference was picking up.
- **00 Project Setup:** The project folder name `Frontend-Forever` has capital letters, which `create-next-app` rejects as an invalid npm package name for the target directory. Worked around by scaffolding into a throwaway temp directory (lowercase name), moving the generated files into this root, and deleting the temp directory. `package.json` name is set to `frontend-forever`. No separate folder was kept — final project lives directly in this root as intended.
- **01 Design System:** `globals.css` defines the full `ui-tokens.md` token set verbatim in `@theme`, plus a small block of shadcn/ui primitive aliases (`--color-primary`, `--color-card`, `--color-muted`, `--color-ring`, etc., and `--radius-4xl` for `badge.tsx`'s pill shape) that point at our semantic tokens instead of shadcn's default oklch palette. This keeps installed primitives on-brand without ever editing files in `components/ui/`. If a newly-added shadcn component references a semantic class not yet aliased, add the alias in `globals.css` rather than hardcoding a color in the component.
- **01 Design System:** Theme persistence uses `useSyncExternalStore` in `ThemeProvider`, not `useState` + `useEffect`. An effect that calls `setState` synchronously to "correct" SSR state trips the `react-hooks/set-state-in-effect` ESLint rule and is the wrong tool for syncing with an external mutable source (DOM class / localStorage) that can legitimately differ between server and client snapshots. `useSyncExternalStore`'s `getServerSnapshot` (`"light"`) vs `getSnapshot` (real DOM class) split avoids both the lint error and any hydration mismatch — the anti-FOUC script in `layout.tsx` (via `next/script`, not a raw `<script>` tag — raw tags inside a Server Component log a console error) sets the real class before hydration, and a `MutationObserver` notifies React of the change.
- **01 Design System:** Hit a stale Turbopack dev cache during verification — `globals.css` was fully rewritten but the served CSS still reflected the old shadcn token mapping (`.text-accent { color: var(--accent) }` instead of `var(--color-accent)`). Fixed by killing the dev server and deleting `.next` before restarting. Worth trying first if styles look wrong after an unusually large `globals.css` change and the file content looks correct on disk.

---

## Notes

- **02 Navbar + Footer:** `lucide-react@1.20.0` (the version pinned in `package.json`) ships no brand/logo icons at all — no `Github`, `Twitter`, `Linkedin`, etc. Verified by listing the package's exports directly. Used the generic `XIcon` glyph for "Follow on X" in both Navbar and Footer since it's the only icon that visually doubles as the X (Twitter) mark. Check `Object.keys(require('lucide-react'))` before assuming a brand icon name exists.
- **02 Navbar + Footer:** Built on `feature/01-design-system` (not a fresh `feature/02-navbar-footer` branch) because `develop` doesn't have Feature 01 merged yet and the Navbar/Footer depend on its tokens/components. Branching and commit deferred per explicit user instruction — resolve the Feature 01 → develop merge before opening Feature 02's PR.
- **02 Navbar + Footer:** User flagged the Navbar didn't match `designs/hero-section-1-event-loop.png` and wasn't satisfied. Verified by pixel-sampling the reference PNG directly (Python/PIL, sampling darkest pixel per region) rather than eyeballing — confirmed nav link text/icons and the "Follow on X" text are near-black (`text-text-primary`), not the muted `text-text-secondary` gray the component originally used. Pill border-radius (`rounded-2xl`), icon choices, and button styles were already correct against the design. Fixed in `Navbar.tsx` (`NavbarPillLink`, `NavbarMobileLink`, "Follow on X" anchor) and corrected the same wrong claim in `ui-rules.md`/`ui-registry.md`. Verified visually via a Playwright screenshot of the running dev server cropped to the navbar, compared side-by-side against the design crop.
- **02 Navbar + Footer (round 2):** Same design, second pass — user pointed out the pill lacked Y padding, the wordmark was too big relative to `FF`, and the pill's shadow/border were too sharp/solid. Pixel-measured the design: `FF`'s cap-height is ~3x "Frontend Forever"'s. Changed logo to `text-3xl`/`text-sm` (was `text-2xl`/`text-lg`), pill container to `px-1.5 py-2.5` (was `p-1.5`) with `border-border-light` + `shadow-xl` (was `border-border` + `shadow-md`) for a softer, more diffused look. `border-border-light` (`#F3F4F6`) is an existing `ui-tokens.md` token, not a new one. Re-verified with the same Playwright-screenshot-vs-design-crop method. Setup note: no project skill for running this app yet — used `npx playwright@1.61.0` installed into a scratch dir outside the repo (`C:\tmp_pw`) since the project itself has no Playwright dependency; worth generating a `/run` skill if this recurs.
- **02 Navbar + Footer (round 3):** User pointed out the landing page background and navbar background read as the same color. Re-checked the design pixel-by-pixel along the header's bottom edge (away from the pill) and found no border line or color step anywhere — the design's header has no chrome of its own; only the floating pill has a visible surface/border/shadow. Removed `border-b border-border` and changed the header from `bg-surface` to `bg-background` to match, so the page and header are intentionally the same color, exactly like the reference. (The header also already had `sticky top-5`, not `top-0` — navbar floats with a gap from the viewport edge — which is what made the previously-identical-but-technically-different `bg-surface`/`bg-background` colors visible as a seam in the first place.)
- **02 Navbar + Footer (round 4):** Round 3's fix used `--color-background` (#FAFAF9, off-white) on the assumption that the page and header should match each other. Pixel-sampling the full reference PNG (PowerShell `System.Drawing`, not just the header edge this time) showed the *entire* canvas — page margins, gaps between sections, everywhere — samples as pure `#FFFFFF`, identical to the pill's `bg-surface`. So `--color-background` was never actually used in the delivered design; the whole light-mode page is `bg-surface`. Changed `Navbar.tsx`'s header and `page.tsx`'s outer container from `bg-background` to `bg-surface` (scoped to these two files only — `--color-background`/`ui-tokens.md` left untouched, since other future pages may still legitimately want the off-white token; user chose this scoped fix over a global token change). Verified via Playwright screenshot of the running dev server — navbar, header, and page now read as one continuous white, matching the design.
  - Follow-up bug from the same round: `header` uses `sticky top-5`, and `position: sticky` immediately offsets an element away from the viewport edge to satisfy its `top` value — even at scroll position 0, with no scrolling needed. That offset leaves an empty gap in the document flow above the header, which is painted by whatever is behind it: `<body>`'s own background, still `bg-background` at the time. Fixed by changing `body { @apply bg-background ...}` to `bg-surface` in `globals.css`'s `@layer base` block — `<body>` is shared root chrome like the navbar, not per-page content, so this is consistent with the round-4 scoping (not a `--color-background` token edit). Needed another dev-server restart + `.next` cache clear to see the change (same stale-Turbopack-cache issue as the note in Feature 01) — re-verified via pixel sampling the new screenshot: gap and header both read `255,255,255`.
- **03 Hero Section (Static):** Ran the `architect` skill before building — aligned on "static" (full visual fidelity, zero interactive logic; entrance motion only) and resolved two scope decisions with the user: (1) the simulator panel placeholder is a full static replica of the design's Event Loop panel, not the build-plan's literal minimal "card with label + badge" wording, since design screenshots are the source of truth and Feature 09 will later swap in the real interactive engine on the same layout; (2) the 4 concept switcher tabs have no `onClick` in this feature — only Event Loop has any content, so making the other 3 "clickable" with nothing to switch to would be misleading. Real switching is Feature 13.
  - New component location: `src/components/homepage/` (sibling to `layout/`/`shared/`), not `src/features/` — homepage sections are page-level UI, not self-contained features like the simulators. `Hero.tsx`, `ConceptSwitcherTabs.tsx`, `HeroSimulatorPreview.tsx`. See `ui-registry.md` for full details.
  - Headline was first built at the build-plan's literal 56–72px and wrapped onto 2 lines — wrong against the design, which fits it on one line. Diagnosed by resizing the full-resolution design PNG (2731×4096) down to the screenshot's working width (1440) and comparing cropped regions directly, then iterating Tailwind size classes against fresh Playwright screenshots until it matched (down to `text-4xl sm:text-5xl lg:text-5xl` + `max-w-6xl`) — same iterative pixel-compare method as the Navbar fixes, not a one-shot calculation. `ui-rules.md` corrected.
  - Concept switcher tabs were first built as a pill-group with gaps (per the original `ui-rules.md` wording) — wrong against the design, which pixel-inspection at full resolution showed to be one continuous bordered bar split into 4 equal segments by vertical dividers, with the active segment getting a `text-accent` + bottom-border indicator and inactive segments in near-black `text-text-primary` (not muted gray) — same "inactive isn't actually muted" lesson as the Navbar's nav links in Feature 02. `ui-rules.md` corrected.
  - Simulator panel column colors (Call Stack/Web APIs/Microtask Queue/Task Queue = purple/green/blue/orange) were missed entirely on the first pass (everything was gray + teal-active). Caught by cropping the full-resolution design rather than relying on the downscaled thumbnail, which made the per-column color-coding and status icons (checkmark vs. dot) clearly visible. Since `ui-tokens.md` has no generic "purple"/"blue" token, reused the closest existing semantic tokens (`premium`, `success`, `info`, `streak`) outside their original gamification meaning, scoped to this one component — documented in `ui-registry.md` so it isn't mistaken for those tokens' usual semantics elsewhere.
  - Reused the scratch Playwright install at `C:\tmp_pw` from Feature 02 (still present) for screenshots — no project Playwright dependency exists yet. Also hit a stale dev-server-on-port-3000 from a previous session returning HTTP 500; killed it and started fresh (same category of issue as Feature 01's stale Turbopack cache — when a previously-started dev server misbehaves, kill and restart rather than debugging the stale instance).
  - **Follow-up:** after the pixel-accurate continuous-divided-bar tabs were built and verified, the user said they preferred the original pill-group concept instead. Reverted `ConceptSwitcherTabs.tsx` to the pill-group style and corrected `ui-rules.md`/`ui-registry.md` to document this as a deliberate user-requested deviation from `designs/hero-section-1-event-loop.png`, not a mistake — the design's actual tab bar is still the continuous divided style, on record in case a future pass wants to revisit it.
  - **Follow-up 2:** asked whether to revert `HeroSimulatorPreview.tsx`'s colored per-column theme back to the original flat single-accent version too (same pattern as the tabs) — user chose to keep the colored version. That is the confirmed-current state of this component, not a pending revert.
  - **Follow-up 3:** added a fake browser-window chrome bar (`BrowserChromeBar`) above the "LIVE CONCEPT ENGINE" header row, per explicit user request — not present in the design screenshot. 3 traffic-light dots (`bg-error`/`bg-warning`/`bg-success`, since no project token is literally named red/yellow/green) + a centered `frontendforever.dev` address-bar pill with a `Lock` icon. Documented in `ui-registry.md`.
- **02 Navbar + Footer (round 5):** User flagged dark mode as flat/low-contrast and asked for a true black backdrop that's still easy on the eyes, with the navbar/buttons reading more visibly. Root cause: round 4 made `body`/`Navbar` header/`page.tsx` use plain `bg-surface` to pixel-match the light-mode reference (where the whole canvas is one white) — but that same single-token treatment carries into dark mode, where `--color-surface` (`#1A1A1A`) was the *only* tone in play, so the page, header, pill, and card all rendered identically with zero hierarchy (the light-mode design relies on `shadow-xl` for separation, which barely registers on a dark background). Light mode has a real reference screenshot proving "everything is one white" is correct; dark mode has no such reference, and the user's direct feedback is that dark mode specifically needs the opposite — a darker backdrop than its elevated surfaces, which is standard dark-theme practice (e.g. Material's elevation system) since shadows don't carry the separation job in the dark.
  - Fix: kept `bg-surface` in light mode but added `dark:bg-background` on `body` (`globals.css`), `Navbar.tsx`'s header, and `page.tsx`'s outer container — these three now diverge from the pill/card's `bg-surface` only in dark mode. Darkened `--color-background` (dark) from `#111111` to `#0A0A0A` for a true near-black that's still a hair off pure `#000000` (avoids the harsher max-contrast halation against light text that reads as "hurts the eyes"). Lightened the dark border tokens for visibility against the new darker backdrop: `--color-border` `#2A2A2A → #333333`, `--color-border-light` `#222222 → #292929`, `--color-border-muted` `#333333 → #404040`. `--color-surface`/`secondary`/`tertiary`/`elevated` untouched — they didn't need to change since contrast comes from the backdrop getting darker, not from them getting lighter. Mirrored all dark-token changes in `ui-tokens.md` and corrected `ui-rules.md`'s navbar section (still described the old single-token light+dark `bg-background` header from before round 4). Verified via Playwright screenshot of the dev server with `localStorage.theme = 'dark'` + pixel-sampling: header/page sample `#0A0A0A`, pill/card/footer sample `#1A1A1A` — a clear, deliberate two-tone split instead of one flat color.
  - Follow-up: user wanted the footer specifically to match the body/header backdrop in dark mode instead of standing out as a `bg-surface` block — added `dark:bg-background` to `Footer.tsx` alongside its existing light-mode `bg-surface`, same pattern as the header/body/page-container fix above. The `border-t border-border` is now the only thing separating footer from page content in dark mode (as in light mode). Re-verified via pixel sampling: footer samples `#0A0A0A`, matching body/header exactly.
- **04 Company Logos Strip:** Ran the `architect` skill before building. Two decisions resolved with the user up front: (1) pixel-inspecting `designs/hero-section-1-event-loop.png` showed the logo strip actually sits *inside* the hero flow, between the CTAs and the concept switcher tabs — not as a separate page-level section below the whole hero block, which is what the build-plan's feature-by-feature numbering implies. Built it that way (editing `Hero.tsx` from Feature 03 to insert `<CompanyLogosStrip />` and shifting the existing motion stagger for tabs/simulator/scroll-indicator back by ~0.4s to make room), matching the design over the literal build-plan structure — same precedent as Feature 03's own headline-size and tab-style corrections. (2) Feature 03 isn't merged to `develop` yet (no PR opened), so `feature/04-company-logos-strip` branches off `feature/03-hero-section` instead of `develop`, since `Hero.tsx` only exists there.
  - The 6 logos (build-plan listed Google, Meta, Amazon, Microsoft, Stripe, Airbnb) are pixel-sampled as **monochrome** in the design (near-black, `text-text-primary`), not the companies' real brand colors — built as simplified inline-SVG/text components in `src/components/shared/CompanyLogos.tsx` using `currentColor`, not static files in `public/logos/` as build-plan literally says, so they correctly invert in dark mode. Same reasoning as `XLogo` from Feature 02.
  - Caught during visual verification (not by typecheck/lint, which both passed clean before this fix): on mobile the logo row silently overflowed the *page* instead of scrolling internally. Root cause — `Hero.tsx`'s section is `flex flex-col items-center`, and the wrapping `<div>` around `CompanyLogosStrip` (and the component's own root `<div>`) lacked `w-full`; without it, a flex item's cross-axis size shrinks to its content's intrinsic width instead of the viewport's, so the scrollable row never actually became wider than its own box and nothing scrolled. Fixed by adding `w-full` at both levels, matching the pattern `HeroSimulatorPreview`'s wrapper already used. Verified via Playwright screenshots (desktop light/dark, mobile 390px) — confirmed via direct DOM measurement (`scrollWidth` 671 vs `clientWidth` 342 on mobile) before and after the fix, not just visually. Any new full-bleed child added to `Hero.tsx` needs the same `w-full` treatment or it will hit this same silent-overflow bug.
  - **Follow-up:** user supplied real brand SVGs one at a time post-build and asked for each placeholder logo to be swapped in (recolored to `currentColor` to stay monochrome): Google, Meta's icon, Amazon, Stripe all replaced with real path data. One supplied "Meta wordmark" SVG was caught as garbled/mismatched before integration — rendering it standalone first showed it actually spelled "heton" with an unrelated two-circle icon, not "Meta" — so it was rejected rather than wired in; render any future supplied SVG standalone before trusting it. Airbnb was then removed entirely and replaced with Anthropic (real SVG, `fillRule="evenodd"`) per explicit user request, and Cursor was added as a 7th logo (icon + wordmark, two real SVGs) on top of the build-plan's original 6 — the row is now Google, Meta, Amazon, Microsoft, Stripe, Anthropic, Cursor, diverging significantly from build-plan's original 6-company list. All 7 still fit on one line on desktop and the mobile scroll/mask treatment still holds — re-verified visually after each addition.
  - **Follow-up (sizing bug):** user asked if there was an alignment issue. Measured via `getBoundingClientRect()` in the live DOM rather than eyeballing — all 7 logos were already correctly vertically centered (`items-center` working as intended, centers within 2px of each other), but rendered heights ranged from 16px (Anthropic, Cursor, both `h-4`) to 20px (Google, Amazon, Stripe, already `h-5`) to 28px (Meta, Microsoft — driven by Tailwind's `text-xl` default 28px line-height on their text labels, not their icon size). The 28px outliers were fixed by adding `leading-none` to Meta's and Microsoft's text spans, collapsing them back to ~20px to match the rest of the row. Anthropic and Cursor were left at 16px in the final pass — smaller than the other 5 by deliberate choice during cleanup, not an unresolved bug. See `ui-registry.md` for current per-logo sizing and how to bump those two to match if a future pass wants full uniformity.
  - **Follow-up (`/review`):** ran the review skill before PR. Found 3 issues, all fixed:
    1. `CompanyLogos.tsx` bundled 7 unrelated components in one file, against `code-standards.md`'s "one component per file" rule and the project's own `XLogo.tsx` precedent. Split into `src/components/shared/logos/` — one file per logo (`GoogleLogo.tsx`, `MetaLogo.tsx`, `AmazonLogo.tsx`, `MicrosoftLogo.tsx`, `StripeLogo.tsx`, `AnthropicLogo.tsx`, `CursorLogo.tsx`).
    2. `AmazonLogo`'s hardcoded `id="amazon-logo-a"` was a latent DOM-id-collision risk if the shared component were ever rendered twice on one page. Replaced with React's `useId()` so each render gets a guaranteed-unique id.
    3. The logo row was almost entirely invisible to screen readers — verified by reading the live accessibility tree, not just visually: 5 of 7 logos had zero accessible name (pure decorative SVGs with no fallback), and the other 2 leaked their visible text inconsistently. Fixed by making every logo's own markup fully `aria-hidden` and adding one `<span className="sr-only">{name}</span>` per logo in `CompanyLogosStrip.tsx`, so every brand name is announced exactly once regardless of how that logo is built internally. Re-verified via DOM inspection (each wrapper has exactly one un-hidden `sr-only` name + one `aria-hidden` logo root) and confirmed zero visual regression via screenshot diff.
- **05 How It Works Section:** Ran the `architect` skill before building. User explicitly asked whether this section's motion was inspired by Notion/Linear's interactive marketing sections — it wasn't (every existing homepage section just uses `whileInView` + manual delay stagger) — and chose to elevate it: scroll-linked motion via Framer Motion's `useScroll`/`useTransform`, the first use of that pattern in this codebase, over the alternative of adding hover micro-interactions on top of the existing simple stagger (explicitly deferred, to keep scope contained). Two other decisions resolved with the user up front: (1) keep the section's height natural/compact like other sections rather than artificially tall to stretch out the scroll-scrubbed reveal; (2) keep a connector between steps on the mobile vertical stack (reoriented vertically) rather than dropping it, since the connecting motif is what signals "this is a sequence."
  - No design screenshot exists for this section (`designs/` only has hero variants + Learn page) — built directly from `build-plan.md`'s text spec: 5 steps (Understand/Simulate/Challenge/Interview/Build), each `icon + title + 1-line description`, no card background, horizontal-with-arrows on desktop / vertical-stack on mobile.
  - **Scroll-offset bug caught during verification, not by typecheck/lint:** the first `useScroll({ offset: ["start 0.8", "end 0.35"] })` attempt made the last step (and its connector) permanently stuck at `opacity: 0` no matter how far down the page was scrolled. Root cause: this is currently the last homepage section before a short `Footer` (Features 06–08 don't exist yet), so the page's total scrollable distance ran out before the section's bottom edge could physically reach 35% from the top of the viewport — the math demanded more scroll room than the page had. Diagnosed by reading each step's live inline `style.opacity`/`style.transform` via Playwright at a series of `window.scrollTo()` positions (not by eyeballing screenshots), which showed steps revealing correctly up to a hard ceiling that matched the page's actual max scroll position. Fixed by loosening the offset to `["start 0.9", "end 0.7"]`, which completes well within the currently-available scroll room with margin to spare. If a future section is added between this one and the footer, this has more headroom than it needs today — but if this `useScroll` pattern is reused on another section that also happens to sit near the bottom of a short page, re-verify the same way rather than assuming the same offset values transfer.
  - Confirmed via Playwright DOM inspection that this feature introduces no overflow: the only `scrollWidth > clientWidth` offenders on mobile belong to Feature 04's `CompanyLogosStrip` (its intentional internal `overflow-x-auto` scroll row), not to anything in `HowItWorksSection`.
  - **Follow-up (full redesign, same session, before any PR):** after the v1 layout above was built and verified, the user shared a screenshot of a Notion marketing-site section ("Keep work moving 24/7") — a left vertical list of selectable items (one expanded with a description) paired with a right-side live-preview panel that swaps content per selection — and asked to build something like that instead. Three scope decisions resolved with the user before rebuilding: (1) replace the v1 row/stack layout entirely rather than keep both (the row and the switcher cover the same ground; keeping both had no clear justification); (2) the right-side preview content per step is a simple static placeholder mockup for each step (no real simulator/challenge/interview UI exists yet — those are Features 09–26), not a reuse of `HeroSimulatorPreview`'s specific visual frame; (3) no autoplay/pause control for this pass — click-to-select only, matching "UI first" and keeping this PR's scope contained; autoplay is a possible later fast-follow.
    - The entire v1 implementation (scroll-linked `useScroll`/`useTransform`, the 9-slot domino reveal, `StepItem`/`StepConnector`) was deleted, not kept behind a flag or commented out — it no longer matches the chosen interaction model (select-one-see-its-demo vs. reveal-all-in-sequence), so there was nothing in it worth preserving. `HowItWorksSection.tsx` is now a two-panel selector shell; the 5 step "Preview" components live in a new `homepage/how-it-works/` subfolder (one file each, same precedent as `shared/logos/`), each a small static mockup foreshadowing its eventual real tab from `build-plan.md` (Feature 09's engine, Feature 24's Challenge Tab, Feature 25's Interview Tab, Feature 26's Build Tab) so that swapping in the real thing later only touches one file, not the switcher shell.
    - Re-verified the same way as v1: typecheck/lint clean, Playwright screenshots at desktop/mobile × light/dark, plus clicking through all 5 steps to confirm the crossfade transition and the active-item description expand/collapse both work, and a repeat of the mobile `scrollWidth`/`clientWidth` check (still only the pre-existing `CompanyLogosStrip` offender, nothing new).
  - **Follow-up 2 (second full redesign, same session, before any PR — v2 → v3):** user said v2 still wasn't it, then described the actual target in detail: 5 separate full sections (not one switcher card), alternating left/right per step, colorful animated SVG visuals, and "texts moving doing something" — specifically per-letter kinetic typography where a word's two repeated letters (e.g. "Understand"'s two `d`s) fly past each other in a parabolic arc and swap places, with other letters flipping/rotating in. Ran the `architect` skill's question-by-question style directly (no separate subagent) since the bulk of remaining unknowns were creative/scope decisions only the user could resolve, not codebase unknowns. Resolved up front: (1) **no GSAP** — `architecture.md` commits to Framer Motion only, and the user's own "shouldn't hang, lightweight" requirement argues against a second animation runtime when Framer Motion already covers everything described; (2) **no bespoke character illustrations** — Notion's actual illustrations are commissioned art, not something reproducible via hand-written SVG; built simple custom shape/icon compositions in brand colors instead (upgraded v2's preview cards rather than starting over); (3) **one generic letter-swap algorithm, not 5 manually curated word choreographies** — among a word's repeated letters, swap the pair with the largest gap between occurrences (tie-broken by whichever starts later) — verified by hand-tracing all 5 words that this mechanically reproduces the user's own "Understand" example (the two `d`s) with zero special-casing; (4) one-shot `whileInView` entrances per section, not continuous scroll-linked `useScroll` — directly answers "shouldn't hang" and avoids re-hitting the exact class of scroll-offset fragility already learned the hard way in v1.
    - v2's entire two-panel switcher shell (`activeId` state, `StepListItem`, the `AnimatePresence mode="wait"` crossfade) was deleted — same "no value preserving code that doesn't match the chosen interaction model" reasoning as the v1→v2 transition. The 5 `*Preview` components were kept and upgraded (enlarged, given one motion touch each) since their actual visual content was already approved at small scale, not thrown away.
    - **Real bug caught during mobile verification, not visually obvious from most screenshots:** the first letter-animation implementation gave each individual letter its own `whileInView`/`viewport` trigger. On a mobile screenshot, "Simulate" rendered as "ulate" — its first 3 letters ("S", "i", "m") were permanently invisible. Direct DOM inspection (`getComputedStyle`/inline `style.opacity`/`getBoundingClientRect`, the same technique used for the v1 scroll-offset bug) showed those letters frozen at `opacity: 0` with `transform: rotateX(-90deg)` and, critically, a **rendered bounding-box height of exactly 0px** — `rotateX(-90deg)` with no `perspective` set fully foreshortens the element's projected height to zero. A zero-area element can fail to ever register as "intersecting" with an IntersectionObserver, so `whileInView` never fired for those specific letters — non-deterministically, since sibling letters on the exact same line (`u`,`l`,`a`,`t`,`e`) happened to trigger fine. This is the kind of bug that "looks fine" in most screenshots (other words/breakpoints didn't happen to hit it) and only showed up reliably via direct state inspection. Fixed by restructuring `AnimatedHeading` to put the `whileInView`/`viewport` trigger **only on the parent container** (always a healthy, non-degenerate bounding box) and propagating the reveal to letters via Framer Motion `variants` + `transition: { staggerChildren }` — the idiomatic pattern for staggering a group of children, and one that never depends on a tiny/transformable child correctly self-reporting its own visibility. Also softened the flip angle from `-90deg` to `-60deg` as a second safety margin. Re-verified via the same direct-DOM-inspection method (not just re-screenshotting) that all letters in all 5 words settle to `opacity: 1` on both mobile and desktop. **Lesson for any future per-letter/per-item entrance animation in this codebase: observe the parent, never each tiny child.**
    - Re-verified: typecheck/lint clean; Playwright screenshots desktop/mobile × light/dark for all 5 sections confirming correct alternation (text/visual flips every other step) and settled letter animations; repeated the mobile `scrollWidth`/`clientWidth` overflow check (one transient 719px reading turned out to be a hot-reload artifact caught mid-recompile — re-running the identical check immediately after gave a clean, consistent 440px, the same pre-existing `CompanyLogosStrip` baseline as v1/v2, confirming no real regression).
  - **Follow-up 3 (visual-polish pass on v3, same session, before any PR):** user rejected the colorless v3 outright as "ugly" and said to use my own judgment rather than asking more clarifying questions. Diagnosed the likely cause directly rather than re-asking: every preview card used the same flat `border border-border bg-surface-secondary` treatment regardless of step, and every icon/accent was plain black or one repeated teal — reading as a gray settings-panel UI, not a colorful illustrated landing page, despite the right alternating structure. Fix: assigned each of the 5 steps one of the existing semantic tokens purely for hue — Understand=`accent`, Simulate=`info`, Challenge=`premium`, Interview=`success`, Build=`streak` — same "reuse a semantic token outside its literal meaning, scoped to one component" precedent already established by `HeroSimulatorPreview`'s 4-color column treatment in Feature 03. Applied that theme color to: a new large rounded icon badge per section (`size-16 rounded-2xl`, theme `bg-{color}-light/muted` + `text-{color}`), each preview card's outer shell (swapped flat `border + bg-surface-secondary` for borderless `bg-{color}-light/muted` + `shadow-sm`, dropping borders project-wide in this section in favor of tint+elevation), a new decorative blurred color blob (`blur-3xl opacity-20`) behind each visual panel for depth, and the heading's swap-letters (now rendered in the step's theme color instead of plain black, tying the motion and color systems together). Semantic status colors inside cards (pass/fail green/red, "Marked as understood" check) were deliberately left alone — only the card shell, badge, blob, and swap-letters take the theme color.
    - Re-verified: typecheck/lint clean; Playwright screenshots desktop/mobile × light/dark confirming each step reads as a distinct, vibrant color family in both themes; repeated the overflow check — same intermittent 440/510/627/719 fluctuation as before, traced this time (not just re-run-and-hope) to the exact same pre-existing `CompanyLogosStrip` logo elements every single time across 5 repeated fresh loads, never anything from this redesign, confirming it's a known dev-server (Turbopack HMR/style-injection) timing race already documented elsewhere in this file, not a regression.
  - **Follow-up 4 (second polish pass on v3, same session, before any PR; Auto Mode active — proceeded on reasonable judgment without re-asking):** user gave three pieces of feedback at once: (1) section-to-section alignment looked inconsistent; (2) still needed more "creative and illustration," not just colored cards; (3) wanted continuous animation "every 2 second" instead of one-shot scroll reveals, for a more attractive/interactive feel.
    - **(1) Alignment root cause:** `StepSection`'s root flex container used `items-center`, vertically centering the text column against the visual column. Since `SimulatePreview` is taller than the other 4 preview cards (extra status row + controls row), centering shifted that section's icon badge/heading to a different vertical start point than the other 4 sections, which all centered against shorter, more similar-height cards — reading as "randomly" inconsistent even though every section uses the identical structure. Fixed by changing to `lg:items-start` so every section's text and visual columns share a top edge regardless of content-height differences on either side. Confirmed via screenshot comparison across all 5 steps that the icon badge and the top of each preview card now align consistently.
    - **(2) More illustration:** added two small `aria-hidden` floating decorative shapes per section (a `Sparkles` icon top-right and a `Plus` icon bottom-left of each visual panel, both in the step's theme color) — a deliberately lightweight way to add "illustrated" visual interest without bespoke art (still no GSAP, no external assets, consistent with constraints already settled in the v2→v3 redesign).
    - **(3) Continuous motion:** added the first **infinite-loop** (not one-shot `whileInView`) Framer Motion animations in this codebase — `animate` (not `whileInView`) with `transition: { duration: 2, repeat: Infinity }` on: each icon badge's icon (gentle scale+rotate), the two floating decorations (float/rotate), and one signature accent per preview card (Understand's lightbulb pulse, Simulate's dot now loops back-and-forth forever instead of animating once, Challenge's code block gained a blinking text cursor + pulsing pass/fail badges, Interview's thumbs-up wiggles, Build's terminal icon wiggles + button pulses). Kept deliberately small in count (1–3 looping elements per section, simple transform/opacity tweens only) specifically because of the project's own "shouldn't hang, lightweight" requirement from earlier in this same redesign — infinite loops are more expensive than one-shot animations by definition (they never stop), so scope was kept tight rather than animating everything continuously.
    - Re-verified: typecheck/lint clean; Playwright screenshots desktop/mobile × light/dark confirming consistent alignment and visible decorations/loops; repeated the overflow check (clean 440px baseline, same as always, the new absolutely-positioned decorations don't escape their relative parent).
  - **Follow-up 5 (custom icon assets, same session, before any PR):** user shared a reference image of a hand-drawn/doodle-style lightbulb icon (thick uneven outline, flat color fill) and asked to use illustration like that instead of lucide icons. Checked first whether I could generate matching art myself — confirmed I have no image-generation tool in this environment, only code editing — and researched where this style typically comes from (IconScout's and Flaticon's "hand drawn doodle" collections, ~20k+ icons each) since grabbing assets from those marketplaces without the user picking a specific licensed pack wasn't something I should do unilaterally. User opted to generate all 5 icons externally themselves (lightbulb, sync arrows, trophy, chat bubble, wrench) using an AI image tool, using color specs I gave matching the existing per-step theme tokens (e.g. `#0D9488` teal for Understand, `#2563EB` blue for Simulate, etc.), and placed the resulting files in `public/icons/how-it-works/`.
    - The 5 delivered files were genuine vector SVGs (verified by inspecting for embedded base64 raster data — none found) but each 104–196 KB, far larger than a normal icon SVG. Inspecting unique fill counts (82 for the bulb vs. 900–1100+ for the other 4) showed why: the bulb is pure grayscale (no color at all, inconsistent with the brief), and the other 4 lean toward their correct theme hue but have hundreds-to-thousands of near-duplicate fill shades — an artifact of however the source was vectorized/traced, not deliberate shading. Flagged both findings to the user rather than silently working around them: asked whether to accept the monochrome bulb as-is or have it regenerated (kept as-is, reads fine against the teal badge) — the hue-but-not-flat-color issue on the other 4 wasn't blocking enough to need a decision, just noted.
    - Wired all 5 into `HowItWorksSection.tsx`'s icon badges via `next/image` with the `unoptimized` prop — Next's built-in image optimizer rejects SVG input by default for security (`dangerouslyAllowSVG` would require a `next.config.ts` change with real XSS tradeoffs for a feature that doesn't need raster optimization anyway, since vectors don't benefit from format conversion/resizing); `unoptimized` sidesteps that while still satisfying `eslint-config-next`'s `no-img-element` rule, which a plain `<img>` would have failed. Removed the lucide `Lightbulb`/`ArrowLeftRight`/`Trophy`/`MessageCircle`/`Wrench` imports and the `icon: LucideIcon` field from `STEPS` entirely — replaced with `iconSrc`/`iconWidth`/`iconHeight` per step (the floating decorative `Sparkles`/`Plus` from the previous follow-up stayed as lucide icons, untouched).
    - **Performance follow-through, not just acceptance:** ~780 KB total across 5 decorative icons was flagged as worth fixing given this redesign's running "shouldn't hang, lightweight" theme — ran `npx svgo --precision 2` on all 5 files in place, a lossless-for-this-purpose optimization (reduces coordinate precision and redundant path data, not visual fidelity at any realistic display size). Got 86–92% reduction per file, ~108 KB total. Verified via before/after Playwright screenshots at the actual rendered badge size (`h-9 w-auto`, ~24–36px) that nothing visibly changed — re-ran the full 5-icon × desktop/dark screenshot sweep, zero console errors, typecheck/lint clean.
  - **Follow-up 6 (badge → glow + bigger icons, same session, before any PR):** user said the icons were "very small" and asked to remove the icon's `rounded-2xl` colored-card background entirely in favor of a glowing color, matching the soft blurred-blob treatment already used behind each visual panel. Replaced the `size-16 rounded-2xl bg-{color}-light` badge with: an `absolute size-28 rounded-full opacity-40 blur-2xl` glow (same recipe as the visual panel's blob, just smaller) sitting behind a much larger bare icon (`h-20 md:h-24`, up from `h-9`). `BADGE_CLASSES` stayed in the file (still used by the floating `Sparkles`/`Plus` decorations) — only the per-step icon's own container changed.
    - **Real bug found and fixed during mobile re-verification, not visible on desktop:** after the resize, the icon's entrance (`whileInView`) was permanently stuck invisible (`opacity: 0`) on mobile (390×844) only — confirmed via direct inline-style inspection (`el.style.opacity`), not a screenshot guess, the same diagnostic method used for the AnimatedHeading zero-height bug earlier in this same feature. Isolated by elimination, in order: (1) checked the sibling description paragraph two lines below, using the identical `whileInView`/`viewport={{ once: true, margin: "-80px" }}` pattern — it triggered fine, ruling out a general viewport-trigger breakdown; (2) suspected the nested continuously-animating icon (`motion.span` with `animate` + `repeat: Infinity`) was somehow interfering with the parent's own trigger — temporarily stripped it entirely, bug persisted, ruling that out too; (3) suspected the `margin: "-80px"` itself, since this is a notably smaller/shorter element than anything else in this codebase using that same convention — removed it (now just `viewport={{ once: true }}`), bug fixed immediately, confirmed across all 5 steps via the same inline-style check. Root cause inside Framer Motion/the browser's IntersectionObserver is unconfirmed (most likely an edge case where a -80px-inset root margin interacts badly with a small/short element on a short viewport), but the fix is verified and isolated to just this one element — every other `whileInView` in the codebase keeps its `margin: "-80px"` unchanged. **If a small, short, or low-on-the-page element ever silently fails to animate in on mobile only, try dropping its `margin` before assuming the bug is elsewhere in the component.**
    - Also hit and resolved separately: the local dev server had stopped responding mid-debugging (unrelated to this bug — environment hiccup) — restarted it (`npm run dev` in the background, polled `curl` until HTTP 200) before continuing.
    - Re-verified: typecheck/lint clean; direct inline-style opacity checks confirming all 5 icons resolve to `opacity: 1` on mobile; full screenshot sweep desktop/mobile × light/dark showing the glow treatment and enlarged icons correctly on both.
  - **Follow-up 7 (`/review`, same session, before any PR):** ran the project's `review` skill against this feature. Found 1 important + 3 minor issues, no critical ones:
    1. **Important — file-size rule.** `HowItWorksSection.tsx` was 229 lines against `code-standards.md`'s explicit 200-line limit. Fixed by extracting the `StepSection` component (layout, icon/glow, theme lookup) into its own `how-it-works/StepSection.tsx`; `HowItWorksSection.tsx` now only holds the `STEPS` data array and the outer section wrapper, down to 97 lines.
    2. **Minor — inline prop types.** `StepSection` and `AnimatedHeading` both defined their props as an inline object type in the function signature, against `code-standards.md`'s explicit rule and against this exact codebase's own established precedent (`HeroSimulatorPreview.tsx`'s `SimulatorColumnProps`/`CodeLineProps`/`ExecutionOrderBarProps`, every file in `shared/logos/`'s `Props`). Fixed — both now use a named `XxxProps` type alias.
    3. **Minor — duplicate color maps.** `BADGE_CLASSES`, `TEXT_CLASSES`, `BLOB_CLASSES` were three separate `Record<ThemeColor, string>` maps keyed identically. Consolidated into one `THEME_CLASSES: Record<ThemeColor, { badge, text, blob }>` in `StepSection.tsx`.
    4. **Minor, not fixed (left as-is, flagged not blocking):** the continuous infinite-loop animations across all 5 sections (icon wiggle + 2 floating decorations + 1–3 signature loops per preview, ~20–30 concurrent loops once a user has scrolled through the page) never pause once triggered, including after scrolling back out of view. No measured performance problem, but worth keeping in mind against this feature's own "shouldn't hang, lightweight" requirement if more looping sections get added later using this as precedent.
    - The review also re-confirmed, as a side effect of its own diagnostic sweep, that the Follow-up 6 mobile bug fix was correctly scoped: checked every other small `whileInView` element using the same `margin: "-80px"` pattern across all 5 preview cards (the "Key Insight" row, the "I knew this"/"Need to review" buttons) and confirmed none of them share the bug — it really was isolated to the one icon container, not a wider pattern that got missed.
    - Re-verified after the refactor: typecheck/lint clean, zero console errors, byte-for-byte-equivalent screenshots before/after on desktop and mobile, and the mobile icon-opacity fix re-confirmed still working post-refactor.
